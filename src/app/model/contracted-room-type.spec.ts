import { ContractedRoomType } from './contracted-room-type';

describe('ContractedRoomType', () => {
  it('should create an instance', () => {
    expect(new ContractedRoomType()).toBeTruthy();
  });
});
